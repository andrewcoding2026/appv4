<?xml version="1.0" encoding="utf-8"?>
<!--
    Auto-backup rules for API < 31 (used when allowBackup is re-enabled).
    Excludes the Room database for the same reason as data_extraction_rules.xml.
-->
<full-backup-content>
    <exclude domain="database" path="NFCSecurity.db" />
    <exclude domain="database" path="NFCSecurity.db-wal" />
    <exclude domain="database" path="NFCSecurity.db-shm" />
</full-backup-content>